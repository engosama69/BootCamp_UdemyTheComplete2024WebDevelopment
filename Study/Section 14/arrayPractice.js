var guestList = ["Angela", "Selina", "Linda", "Carla"];

var guestName = prompt("What is your name?");

if (guestName.includes(guestName)) {
    alert("Welcome!");
}
else {
    alert("Sorry, maybe next time");
}